// You can't define a variable more than once
let petName = 'Sage'
petName = 'Chexiboiiii'

console.log(petName)

// There are rules related to variable names
// - Has to start with letter
// - Can start with $ or _ (these are also the only symbols you can use)
let $3 = 2
let result = $3 + 4

console.log(result)

// variable names cannot be reserved keywords
// let let = 12

// console.log(let) FAILED!!!
